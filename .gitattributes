# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Treat image assets as binary (never line-ending-converted or diffed as text).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
